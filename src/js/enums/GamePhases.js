const GamePhases = Object.freeze({
    IN_LOBBY: 'IN_LOBBY',
    IN_GAME: 'IN_GAME',
    GAME_ENDED: 'GAME_ENDED',
    PAUSED: 'PAUSED', // Optional, if you want to implement pause functionality
});

export default GamePhases;
